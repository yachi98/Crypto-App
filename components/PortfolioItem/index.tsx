import { Portfolio } from "@/interfaces/portfolio.interface";

const PortfolioItem = () => {
  //   const coinData: Portfolio = portfolioCoins.find(
  //     (item) => item.id === portfolio.coinId
  //   )!;
  return <div className="bg-red-500 w-[300px] h-[50px]">\</div>;
};

export default PortfolioItem;
